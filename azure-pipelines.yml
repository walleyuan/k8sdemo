
variables:
  imageName: 'k8sdemo-docker-image'
  
steps:
  - task: PowerShell@2
    displayName: Terrafom Init
    inputs:
      targetType: 'inline'
      script: |
        # run terrafom 
        terraform init
      workingDirectory: '/infra/dev'

